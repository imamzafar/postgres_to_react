# Base URL for FastAPI when running locally without a Vite proxy.
VITE_API_BASE_URL=http://localhost:8000

# Set to "true" to enable the Vite dev server proxy (optional).
# VITE_USE_PROXY=false
# VITE_API_PROXY_TARGET=http://localhost:8000
