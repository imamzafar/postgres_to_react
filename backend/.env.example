# Copy this file to .env and adjust values for your local environment.
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/inventory_db
# Optional granular settings used when DATABASE_URL is not set
# PG_USER=postgres
# PG_PASSWORD=postgres
# PG_HOST=localhost
# PG_PORT=5432
# PG_DATABASE=inventory_db
# Optional admin database configuration used by scripts/create_database.py
# If provided, this DSN (including credentials) will be used to connect to
# the admin database to create the application database. It should typically
# point at the built-in 'postgres' DB.
# ADMIN_DATABASE_URL=postgresql://postgres:postgres@localhost:5432/postgres
# Alternatively, you can omit ADMIN_DATABASE_URL and just set the admin DB name
# to switch into using the same credentials as DATABASE_URL:
# POSTGRES_ADMIN_DB=postgres
